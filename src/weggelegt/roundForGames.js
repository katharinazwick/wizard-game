/*import {setRound} from "./sharedState.js";

export function roundForGames (event) {
    const input = event.target;
    const formCalculateSequence = new FormData(input.form);
    const round = formCalculateSequence.get("calculateSequence");
    setRound(round);
}*/